import { red } from '@material-ui/core/colors';

// A custom theme for this app
const theme = {
  palette: {
    type:"light",
    primary: {
      main: '#556cd6',
    },
    secondary: {
      main: '#f44336',
    },
    error: {
      main: red.A400,
    }
  },
}

export default theme;